package com.mycompany.app;

public class Book {
    /* 
     * Book Object
     *      Create a book object that should be able to be tied to a member and library
     *      The book should have some describing factors: Name, Genre, Num of pages
     *      The book obj should have info on when it was checked out or if it is checked out
     *      This will need to be a join table between library and member
     */
}
